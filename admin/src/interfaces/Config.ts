export interface WebsiteConfig {
  name: string
  icon: string
  post_count: number
  category_count: number
  view_count: number
  live_time: number
  domain: string
  records: string[]
}

export interface OwnerConfig {
  name: string
  profile: string
  picture: string
}
